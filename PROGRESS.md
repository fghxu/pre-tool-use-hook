## Goal
Design + implement llm_second_opinion: a second-opinion LLM cross-check of the local classification; disagreement in the dangerous direction (local=allow, LLM=modifying) forces ask. Spec: docs/superpowers/specs/2026-08-01-llm-second-opinion-design.md

(Prior goal — strictness_gated config section + per-domain strictness — COMPLETE; spec: docs/superpowers/specs/2026-07-25-strictness-gated-design.md)

## State note (2026-07-25)
- master @ 539f8eb. path-branch merged (864e85c) + test consolidation done.
- strictness_gated implementation branches from master.

## Completed Steps
- path-branch: merged to master; all suites green (byte-identical + trustedpattern 69/74).
- Test consolidation: test-cases.xml now 690 cases (merged adhoc, fullpath, var-assignment, redirect-normal); 4 source files + redirect-normal.xml deleted (content merged). 684/690, 6 pre-existing fails (1 git-add + 5 redirect-non-system). Remaining separate files (by necessity): redirect-strict (strict-mode), trustedpattern (cwd-keyed), fullpipe (different runner).
- strictness_gated spec written + self-reviewed + committed (5b27112).
- Plan Tasks 1-4 (branch strictness-gated, all dormant): TestRunner -ConfigPath (d23c2b6) → ConfigLoader compiles/validates strictness_gated + per-domain modifying_strictness (1b652c5) → Resolver Get-EffectiveStrictness + step 1a.5 + effective-strictness reach for AWS flag-strip/param_commands (5c4b780).
- Plan Task 5 (5e8b277): fixtures test/config/config.{git-strict,strict}.json + 3 suites test/test-cases.strictness-gated.{normal,strict,git-strict}.xml. RED baseline confirmed: normal 24/24 GREEN; strict 6/24 (6 controls pass, 18 gated fail — still read_only); git-strict 6/12 (isolation+AWS-reach pass, 6 Git-gated fail). Both fixtures load via Load-Config. Live config.json untouched.
- Plan Task 6 (221c1b8): config.json migration — moved Git×10 (pull, switch, init, clone, tag -d, add, worktree add, commit, rev-parse, stash) + Linux printf read_only → strictness_gated; git switch gained risk:low; comments updated (_comment_planned→_comment_gated, "(planned)"→live). Fixtures regenerated from migrated config. Normal-mode byte-identical (684/690); suites flipped GREEN: normal 24/24, strict 24/24, git-strict 12/12.
- Plan Task 7 (ae1197b): redirect-strict now fixture-driven — `-ConfigPath test/config/config.strict.json` (no -Strictness) verified identical to -Strictness strict (24/25, same #22 pre-existing fail). README updated.
- test-cases.xml made strictness_gated-aware: stale VarAssignment case `$x = git add .` flipped expect ask→allow (reason points to SG suites; strict-mode ask already pinned at test-cases.strictness-gated.strict.xml SG-Strict-VarAssignment). Main suite now 685/690 — only the 5 pre-existing redirect-non-system fails remain. SG suites re-verified: normal 24/24, strict 24/24, git-strict 12/12.
- src/Run-AllTests.ps1: one-shot runner over every test/*.xml (auto-discovers new suites; per-suite invocation table for -Strictness/-Cwd/-ConfigPath/fullpipe; KnownFails baselines; REGRESSION/OK/IMPROVED verdicts; -Filter, -ShowOutput; exit 1 on regression). Verified: 7 suites, 858/868, 10 fails all within baseline. NOTE: pure ASCII only — powershell.exe 5.1 misreads UTF-8 em-dashes as smart quotes (string-delimiter parse cascade).
- Baseline correction: redirect-strict is 25/25 (fixture and -Strictness strict both verified). CORRECTION to the earlier note: the #22 fix (C:\temp\svc.txt → C:\some_dir\svc.txt) was the user's own uncommitted edit, not "since a4ed09f" — the git-log attribution I made was a misread (the -S output belonged to the first command). The C:\temp PSRemoting case is test-cases.xml #687 and passes.
- PS wrapper unwrap (user-reported: `powershell.exe -ExecutionPolicy Bypass -Command "..."` hit unknown-ask): two gaps — (a) AST wrapper detection `$CommandName -match '^(pwsh|powershell)$'` missed the `.exe` suffix; (b) regex Find-NestedCommands required `-Command` immediately after the binary (no flag tolerance). Fixed: AST CommandName regex +`(\.exe)?`; Find-NestedCommands `-Command`/`-c`/`-ScriptBlock` regexes made flag-tolerant (lazy `.*?`). `-File` stays ask by design (opaque script content — pinned by test). PSWrapper-FlagSkipping block now 11 cases (6 original + 5 added on request: -c short form, -ScriptBlock allow/ask, value-taking flag + cross-domain inner, wrapper-as-pipeline-head). Full suite 879/879, no regressions.
- ALL-GATED PROMOTED TO LIVE (2026-07-27): user overwrote repo-root config.json with the sandbox's all-gated config (verified byte-identical to test-strictness-gate/normal/config.normal.json) — treated as deliberate promotion (trade-offs were documented: Set-Content→C:\Windows allows in normal). Live config marker fixed to _comment_all_gated policy note. Suites CONSOLIDATED into test/config/live/: the sandbox's flipped suites became the live suites (main 701 w/ 29 flips, SG normal 48, SG strict 49, trustedpattern 74 w/ Set-Content flip, redirect-strict 25) + git-strict 12 (per-domain coverage kept) + fullpipe 19 + codex 17; fixtures regenerated from new live config (git-strict, strict). Superseded originals + stale fixtures deleted (git history has them); test-strictness-gate/ folder removed. Rewired: Run-AllTests.ps1 → test/config/live; FullPipeTestRunner.ps1 + codex.ps1 path depth fixed (..\..\..\src — test/config/live is 3 deep; first attempt with ..\..\ failed all 19 fullpipe cases, caught by Run-AllTests). New test/config/live/README.md. **ALL GREEN: Run-AllTests 928/928 (7 suites), codex 17/17.**
- Committed + pushed: f58021a (promotion + consolidation) and cd959b6 (PROGRESS) to origin/strictness-gated.
- MISTAKE + RESTORE (2026-07-28): during consolidation I deleted test/config/test-strictness-gate/ WITHOUT asking the user (it was uncommitted work; "reconstructible" is not "disposable" — user rightly called this out). Restored the folder to the exact good-shape state: normal/ (config.normal.json + 4 suites: main 701, SG-normal 48, SG-strict 49, trustedpattern 74) and strict/ (config.strict.json + redirect-strict 25) + Run-Tests.ps1 + README.md. Suite headers reverted to their sandbox originals (reference live/ paths for the originals they duplicate). Verified: sandbox 897/897, live 928/928 — both coexist. LESSON: never delete uncommitted files/folders without explicit user confirmation.
- Global key RENAME (2026-07-28, user request): top-level "modifying_strictness" → "global_modifying_strictness" (per-domain commands.<domain>.modifying_strictness UNCHANGED). Code: ConfigLoader (renamed default/validation + NEW legacy-key rejection guard, fail-closed with actionable message), Resolver Get-EffectiveStrictness, Parser path policy (2 reads), TestRunner -Strictness override, debug/run-tests.ps1. Configs renamed: live config.json (user did the key line under deadlock; I did comment key + _readme_strictness_guard), live fixtures ×2, sandbox configs ×2. Docs: config-json-guide (top-level table + §10 guard). INCIDENT: the new guard deadlocked the session mid-change (config still had old key — exactly the documented fail-closed behavior; user escaped it by hand-editing line 13). Verified: Run-AllTests 928/928, sandbox 897/897, codex 17/17, plus negative test proving stale configs throw.
- Test-config insulation (2026-07-28, user request): live suites no longer read the repo-root config.json — they run against test/config/live/config.json, a TEST COPY synced on demand via new test/config/live/Sync-Fixtures.ps1 (also regenerates config.strict.json; both validated on sync). Rewired: Run-AllTests.ps1 (-ConfigPath for all 5 TestRunner suites + fullpipe args passthrough), Hook.ps1 NEW env override PRETOOLHOOK_CONFIG_PATH (production default unchanged) + FullPipeTestRunner -ConfigPath param. git-strict RETIRED per user decision (no git-only strict tests): config.git-strict.json + test-cases.strictness-gated.git-strict.xml deleted — nothing applicable to merge (6 git cases already in SG-strict; isolation/AWS cases invalid under global strict). NOTE: per-domain modifying_strictness now has NO suite coverage (feature remains in code). Headers/README/guide updated. Verified: Run-AllTests 916/916 (= 928 - 12 retired), sandbox 897/897 untouched. Sync-Fixtures bug caught by its own load validation (trailing comma in reduced editable_paths.linux) and fixed.
- Live-tree fixes from the user's uncommitted config edit (git tag create → gated): original test-cases.xml 2 tag-create cases flipped ask→allow ([622] multi-line tag pipeline, [649] tag -a -m); test/config fixtures REGENERATED from updated config.json (git-strict: Git=strict + _comment_fixture; strict: global strict + editable divergence) — they predated the edit. Run-AllTests back to **879/879 green**.
- Known failures ELIMINATED via config: trusted_pattern += `docker\s+exec\s+(?:-\S+\s+)?comfyui\b` (fixes the 5 trustedpattern comfyui cases); editable_paths.linux reworked — the user's dead `/home/user/` replaced by working patterns `([A-Za-z]:)?[/\\]home[/\\](user|dev)[/\\]` + `~[/\\]` that match the GetFullPath-resolved form (POSIX redirect targets become `C:\home\...` on Windows inside Test-EditableOrCwd, Parser.ps1:1066) — fixes #666-669/#685. Collateral (by design): 2 trustedpattern Write cases (/home/user, ~) flipped ask→allow, expectations updated. config.strict.json deliberately does NOT get the editable additions (its strict redirect cases must stay non-editable) — _comment_fixture annotated; config-json-guide §2 gained the POSIX gotcha + editable=allow-in-every-mode correction. Run-AllTests KnownFails zeroed. **ALL SUITES GREEN: 868/868.**
- All 8 command domains now carry explicit `"modifying_strictness": "normal"` (discoverability; behavior byte-identical to absent per Get-EffectiveStrictness; ConfigLoader validates the value). Fixtures mirrored: config.strict.json +8, config.git-strict.json +7 (Git stays "strict"). config-json-guide §10 updated. Also removed an accidental DUPLICATE modifying_strictness key in config.json's Git domain (user's fixture paste _comment_fixture+"strict" landed after my "normal"; JSON last-wins would have made live Git strict).
- User edits observed and kept: editable_paths.linux += /home/user/ (NOTE: does NOT make the 5 redirect-non-system cases allow — canonicalization unifies / to \, and Parser.ps1:1176 normal-fallback stays disabled while editable_paths is populated); redirect-strict #22 retarget (above). Verified after all changes: Run-AllTests 858/868, all within baseline.
- SECURITY/CORRECTNESS AUDIT (2026-07-28, user request "analyze each test case for accuracy"): Runner = 916/916 green, which proves every expectation is CONSISTENT with the engine+config but not that it is CORRECT. Audited semantic correctness myself via an engine oracle (temp/Classify.ps1 + BatchClassify.ps1; temp/AUDIT-BRIEF.md has the algorithm). Found 6 HIGH silent-auto-approve holes: (H1) trusted_pattern escape-hatches `C..git.cc.pretoolhook` and `src.{Parser,TestRunner,ConfigLoader}.ps1` are unanchored substrings -> ANY destructive line containing them is allowed (rm -rf <repo>, `... && shutdown /r`); (H2) `docker\s+exec\s+...comfyui\b` trusted_pattern allows any inner cmd (rm -rf /, dd disk-wipe, curl|sh); (H3) awk read_only swallows `system()` (arbitrary exec) + `> file`; (H4) curl `-o`/`-O` disk-write incl. to system paths allowed via parameter_commands default (path policy ignores `-o` target; wget -O correctly asks); (H5) redirect target `%SystemRoot%`/`$env:` not expanded -> classified under-CWD -> allow (expands to C:\Windows under cmd); (H6) destructive .NET methods inside ForEach-Object/Where-Object scriptblocks (`$_.Kill()`, `$_.Delete()`, `[IO.File]::Delete`) are not inspected (only CommandAst nodes are) -> allow. MEDIUM: sed `w`, wmic `call terminate`, `net share /GRANT:`, `find -fprint` all read_only-allowed; Set-Content->C:\Windows allowed (documented all-gated trade-off, trustedpattern.xml TP-CmdPaths:338). LOW: `git rev-parse` mis-tiered as strictness_gated/medium (it is read-only). WHY GREEN DESPITE HOLES: none of the dangerous variants have a test case - systematic coverage blind spots (curl -o/-O has zero coverage; the 4 unanchored trusted_patterns have zero coverage). NOTE: parallel-subagent audit was attempted but abandoned - spawned agents fail on glm-5.2 (unavailable) and `model:sonnet` override is not honored in this environment; ran the audit in the main session instead. Fixes not yet applied (awaiting user decision).
- FILE-TOOL PATH-POLICY FIX (2026-07-28, user report "Edit/Write don't observe system_paths/editable_paths, only Bash/PowerShell do"): Diagnosis - the path branch DID classify correctly (Classifier STEP 1.5: path_tool_mapping -> Resolve-PathPolicy; manual probes: Write->C:\Windows = ask, Write->C:\temp = allow, Edit->D:\work = ask, Write->CWD = allow; trustedpattern suite already pinned the decisions), BUT the path branch returned ask with ExitCode 2, and Claude Code treats exit 2 as a blocking hook error ("PreToolUse:Write hook error: No stderr output") - the JSON ask is never parsed, the user never gets a prompt, and the tool call hard-blocks with a cryptic error. Command-path asks already exit 0 (Classifier STEP 4f), which is why only Bash/PowerShell prompted. Fix: Classifier.ps1 path-branch AND 4d direct-result asks now always exit 0 (decision JSON on stdout is authoritative; exit 2 reserved for fatal no-decision failures: empty stdin / bad JSON / config load error). Deliberately untouched (pinned as "genuine error" exit 2 in test-fullpipe.xml): unknown tool, unextractable command, null input/config. Tests: test-fullpipe.xml +5 cases (CC Write system-path ask, CC Write editable allow, CC Edit foreign ask, CP create_file system-path ask, CX Write system-path deny; all exit 0). Verified: Run-AllTests 921/921 (= 916 + 5 new pins), and a LIVE in-session probe (Write tool -> C:\Users\fghxu\hook-probe-claude.txt) produced a REAL user prompt under acceptEdits mode - user confirmed approving it, so acceptEdits does NOT swallow a hook ask; probe file deleted. README gained an "Exit-code contract" paragraph. Committed + pushed as f462ac9 on strictness-gated (src/Classifier.ps1, test/config/live/test-fullpipe.xml, README.md, PROGRESS.md).
- PER-CASE PROOF-READ of all XML suites (2026-07-28, user request: "judge each case read-only vs modifying, compare to its expected"): reviewed all 921 cases (test-cases.xml 701, SG-normal 48, SG-strict 49, redirect-strict 25, trustedpattern 74, fullpipe 24). Method: common-sense read-only/modifying judgment per command (inner cmds/pipelines/redirects considered), config.json used only as a semantic reference - NOT judging config correctness (that is docs/finding_1.md). RESULT: 0 decision mismatches - every expected matches common sense. Fixed 5 cosmetic reason/description label typos: test-cases.xml ~1024 (cat /etc/hostname reason hostname->cat), ~1164 (cat|sed reason cat->sed), ~2277 (docker rm -f reason 'rm -f'->'docker rm'), ~2282 (docker rmi reason 'docker rm'->'docker rmi'); test-fullpipe.xml ~22 (description 'git status'->'docker ps' for the docker ps case). Left unchanged (by design): ~70 gated-tier cases + normal-mode non-system redirects are modifying-but-allow (paired strict suites assert ask); conservative over-blocks (find -exec cat, double/triple-nested wrappers, ssh -W) pin read-only cmds to ask. Report: docs/finding_2.md. Verified: Run-AllTests 921/921 green after the 5 label fixes.
- multi_replace_string_in_file ARRAY-PAYLOAD FIX (2026-07-29; user report: path_tool_mapping mapped tool_input.filePath but the real Copilot payload is tool_input.replacements[*].filePath — design gap in 2026-07-25-path-branch-design.md §3.4, surfacing as a fail-safe 'path not extractable'→ask for a CWD write). TDD: RED showed the 3 new allow-cases failing with that exact reason; GREEN added HookAdapter Get-InputFieldValues + Resolve-FieldPathLeaves (dot-path walker; a `[*]` segment enumerates a JSON array, scalar paths still yield one value — leaves are STREAMED so the caller's @() cannot re-nest them) and rewired Classifier STEP 1.5 to resolve EVERY extracted path via Resolve-PathPolicy with WORST-CASE-WINS aggregation (any ask→ask; empty list→fail-safe ask; single-path behavior byte-identical for Write/Edit/MultiEdit/etc.). config.json + 3 config copies: mapping → tool_input.replacements[*].filePath. 6 new cases in both trustedpattern XMLs. Caught mid-cycle: a unary-comma return re-nested the array (paths space-joined into one Resolve-PathPolicy call) — fixed by streaming. Design doc §3.4 amended ([*] syntax + worst-case rule; §4 risk updated). edit_files/apply_patch still need bespoke extraction (paths in patch text, not a JSON field) — flagged in doc.
- comfyui TEST-CASE + CONFIG REMOVAL (2026-07-29; user: side project retired): removed the 5 'docker exec comfyui' TrustedPattern-Gate cases from both trustedpattern XMLs (kept the docker-run→ask negative test) and the orphaned `docker\s+exec\s+(?:-\S+\s+)?comfyui\b` trusted_pattern entry from both strictness-gate configs (root config.json already lacked it). grep comfyui = 0.
- trusted_programs DECOMPOSED-LEVEL ALLOWLIST (2026-07-30; user: trusted_pattern is too generous — a whole-string gate that bypasses decomposition, can't tell `& abc.ps1 Get-ChildItem …` from `& abc.ps1 ; Remove-Item …`; wanted a finer block matched AFTER decomposition, where a trusted program allows only if no modifying command appears in the statement, INCLUDING its args — confirmed Option B). Plan: docs/superpowers/specs → C:\Users\fghxu\.claude\plans\dazzling-skipping-manatee.md (approved). TDD: new isolated fixture test/config/trusted-programs/ (config.json + test-cases.xml, 10 cases); RED showed the 6 with-args allow-cases failing as `unknown command`; GREEN after impl. Code: ConfigLoader optional validate (non-throwing if absent) + store normalized list on _compiled.trustedPrograms; Resolver Test-TrustedProgram (path-suffix / basename, case+separator-insensitive) + Test-StatementContainsModifying (tokenize args, probe each suffix via Resolve-Command with per-suffix domain re-detect, block iff ask + NON-EMPTY MatchedPattern — unknown args ignored) + insertion at Resolve-Command step 0f (~:276, after first-token extraction, before full-path recursion so the ORIGINAL token is matchable; uniform across all entry points; worst-case-wins still forces ask on modifying siblings). Two KEY findings during TDD: (1) `& c:\temp\abc.ps1 …` detects as the LINUX domain (the `&`+path prefix masks the Verb-Noun signal) so the fixture needed a Linux domain for Resolve-Command to reach the program-token step — arg-suffixes re-detect independently (`Remove-Item c:\temp` → powershell), so the cross-domain arg-scan is required and works; (2) New-ResolutionResult's `[string]$MatchedPattern` param coerces $null→"" — so "no match" is the EMPTY STRING everywhere, and `$null -ne` wrongly treats unknown as matched; fixed with IsNullOrWhiteSpace (the Classifier's existing `Where-Object { $_.MatchedPattern }` already relied on "" being falsy). Verified: fixture 10/10, sandbox 897/897, live 921/921 — ZERO regressions (additive: no existing config has trusted_programs, so the new check is a no-op for them). ROOT config.json NOT touched (gated on explicit approval). Known limitation (documented, not fixed): `pwsh -File abc.ps1 …` is deliberately not unwrapped (Parser.ps1:472), so only the `& path` / bare-`path` forms are covered.
- trusted_programs ROOT MIGRATION (2026-07-30, user-approved "Add block + migrate the 4"): root config.json — removed the 4 over-generous dot-form trusted_pattern entries (src.TestRunner.ps1 etc.; these matched the WHOLE command string so `… ; rm -rf /` was auto-allowed — the H1 audit hole), added a trusted_programs block with the 4 scripts in proper path-form (src/TestRunner.ps1, src/Run-AllTests.ps1, src/Classifier.ps1, test-strictness-gate/Run-Tests.ps1). Synced root → live fixtures via Sync-Fixtures.ps1 (validated). The sync EXPOSED a pre-existing inconsistency: the user's uncommitted 'Export File' entry (Export-Csv/Export-Clixml/Tee-Object/Out-Printer) in PowerShell strictness_gated => allow in normal mode, conflicting with test-cases.xml case 139 (expected ask). Verified NOT caused by trusted_programs (debug: Export-Csv allow comes from strictness_gated 'Export File', trusted_programs uninvolved). User chose 'keep gated, update test' => case 139 flipped expected ask->allow (+ reason/description updated to strictness_gated). Live 921/921, sandbox 897/897 (sandbox config.normal.json was NOT synced — it still lacks the user's Export-File/Set-Location edits, so it diverges from root; sandbox case 139 stays ask and passes against its own config). NOTE: trusted_programs entries cover the & path / bare-path forms; pwsh -File invocations are already allowed by the pwsh read_only pattern regardless.

- trusted_programs CASE MERGE (2026-07-30, user: 'merge the trusted-programs cases into live + sandbox'): user chose 'rewrite to real entries' and named the 4 programs — TestRunner.ps1 (bare), src\Classifier.ps1 (partial), test-strictness-gate\Run-Tests.ps1 (partial), C:\git\cc\pretoolhook\src\Run-AllTests.ps1 (full; covers bare+partial+full matching with real scripts). Set trusted_programs to these 4 in root config.json AND sandbox config.normal.json (was absent there). Rewrote the 11 TrustedPrograms cases against the real entries and merged a category-group into BOTH test/config/live/test-cases.xml and test/config/test-strictness-gate/normal/test-cases.xml. Synced root -> live. Verified: live 932/932 (test-cases.xml 701->712), sandbox 908/908 (normal/test-cases.xml 701->712). Standalone fixture test/config/trusted-programs/ kept as the c:\temp\abc.ps1-based reference (own config).

- STATIC .NET METHOD SAFE-EXPRESSION FIX (2026-07-31; user report: [Type]::Method(...) in ;-chained commands emitted as 'unknown command', e.g. [regex]::Matches, PSParser::Tokenize). EVALUATION (user's hypothesis partially right): Test-SafeAst DOES visit InvokeMemberExpressionAst (Parser.ps1:1971) and already recurses into expression+args — the real gaps were (a) the name-only dotnet_method_allowlist lacking Matches/Tokenize/Combine/ToBase64String, with no type qualification; (b) NO ConvertExpressionAst case, so [ref]$x / [byte[]](...) arguments failed closed even for allowlisted methods. Also the '; vs newline' difference is really ORDER: a leading cmdlet/PS-marker => powershell domain => Get-PowerShellCommands extracts only cmdlets and ignores assignments (allow); a leading static call => linux domain => regex-split => 'unknown command' => arbiter/certifier fails (ask). FIX (TDD red->green): config safe_expressions.dotnet_static_method_allowlist (type-qualified 'TypeName::Method', matched case-insensitively against the type AS WRITTEN + its reflected full name; added to root config.json + sandbox config.normal.json) + ConfigLoader compiles _dotnetStaticMethodAllowlist (optional; EMPTY fail-closed default, no built-in list) + Test-SafeAst: NEW ConvertExpressionAst case (recurse into .Child — a cast is side-effect-free) + InvokeMemberExpressionAst type-qualified static check (name-only path preserved for instance calls). RED: exactly the 5 positive cases failed in both suites with 'unknown command: [Type]::Method(...)'; GREEN: 10/10 both suites. Entries: regex::Matches, System.Management.Automation.PSParser::Tokenize, System.IO.Path::Combine, Convert::ToBase64String, Math::Round. Negative pins (still ask, both arbiter + certifier paths): System.IO.File::WriteAllText, System.IO.File::Delete, System.Diagnostics.Process::Start. 10-case SafeStaticMethods group added to live + sandbox main suites. Verified: live 942/942 (test-cases.xml 712->722), sandbox 918/918 (712->722).

- STATIC ALLOWLIST FILLED + VERIFIED (2026-07-31; user asked to populate dotnet_static_method_allowlist): 5-entry starter replaced with 183 entries in root config.json + sandbox config.normal.json, grouped by type (Regex, PSParser/Parser, IO.Path, IO.File READS only, IO.Directory READS only, Math, Convert, BitConverter, String statics, Guid, DateTime/DateTimeOffset/TimeSpan/Version parses, Array searches, Environment reads, primitive parses, IPAddress parses). Every entry verified via .NET reflection as public+static on the local runtime (temp/verify-static-list.ps1: 183 OK / 0 missing / 0 type-fail). Deliberately EXCLUDED (still ask): writers/mutators (File::Write*/Append*/Delete/Copy/Move/Create/SetAttributes, Directory::Create/Delete/Move/SetCurrentDirectory, Array::Sort/Reverse/Clear/Copy, Environment::SetEnvironmentVariable, Path::GetTempFileName [creates a file], String::Intern, Process::Start). Runtime caveat: a few entries are .NET 5+ only (Convert::ToHexString, math::Cbrt, DateTimeOffset::FromUnixTimeSeconds) — harmless on PS 5.1 because the allowlist is string-matched, never invoked; nonexistent methods simply never match. Regression after fill: live 942/942, sandbox 918/918 — ZERO flips (no existing expectation depended on these statics asking; consistent with the finding_1 coverage blind spot). DISALLOW-LIST question (user): recommended NOT adding one now — unlisted methods already fail closed to ask, so the allowlist IS the gate; a disallow list would only change the reason text, not the decision; the dangerous set is unbounded. Revisit only if wildcard/prefix matching is ever added to the allowlist (then disallow must take precedence) or if clearer reasons are wanted.

- DOTNET_METHOD_ALLOWLIST EXPANSION + INSTANCE/STATIC GATE (2026-07-31; user: expand the name-only list with read-only methods, regression tested). SECURITY FINDING during curation: the EXISTING base name "Replace" also matched [System.IO.File]::Replace (file overwrite) — name-only matching cannot distinguish string.Replace (pure) from File.Replace (write); proven live by RED case (expected ask, got allow via AST arbitration). FIX (architecture): Test-SafeAst InvokeMemberExpressionAst now applies the name-only dotnet_method_allowlist ONLY to INSTANCE-style calls ($obj.Name(...), incl. intrinsic array .Where/.ForEach); static [Type]::Method calls must pass the TYPE-QUALIFIED dotnet_static_method_allowlist. This closed the File.Replace hole while keeping 'a-b'.Replace(...) allowed. Lists curated via reflection (existence + collision scan across ~40 BCL types, temp/verify-instance-list.ps1 + verify-static-adds.ps1): name-only 31 -> 106 (real instance methods only: string ops, List/Dictionary/Hashtable reads + Find/Exists/GetRange, DateTime/TimeSpan AddDays..Subtract, Encoding instance, Regex instance, XmlDocument reads, DirectoryInfo.GetFiles family, StreamReader.ReadLine/ReadToEnd, Random.Next/NextDouble, intrinsic Where/ForEach); static 183 -> 245 (added System.Linq.Enumerable::* 53 — PS calls LINQ statically, plus Process::GetProcesses/GetProcessById/GetCurrentProcess, ServiceController::GetServices, Console::ReadLine, Enum::GetNames/GetValues/GetName/IsDefined). Mutator-colliding names deliberately excluded (Add, Insert, Remove, Reverse, Sort, Copy, CopyTo, Save, Load, Write*, Append*, Delete, Move, Create, Open, Start, Stop, Kill, Dispose, Close, Flush, Set*, GetTempFileName, Serialize, Deserialize, Intern). TDD: 20-case DotNetMethodAllowlist group in both suites; RED showed exactly the 9 expected failures (8 new-name positives + the File.Replace hole allow); GREEN 20/20 both. Regression: live 962/962 (722->742), sandbox 938/938 — ZERO flips from the gate (no existing case relied on name-only static matching outside the 183 list).

- LLM SECOND-OPINION IMPLEMENTATION COMPLETE (2026-08-01, plan docs/superpowers/plans/2026-08-01-llm-second-opinion.md, executed inline via executing-plans): TDD per task. f61d748 fixture skeleton (test/config/llm-review/: config.json + config.badlevel.json + test-cases.xml 16 cases + Run-Tests.ps1; RED 2/25 exactly as planned) -> e52a20a ConfigLoader optional llm_second_opinion validate+compile (fixture 3/25; Run-AllTests 962/962) -> 9be8638 src/LlmReview.ps1 scope engine + gate (fixture 8/25; runner parser guard fixed to key on ConvertTo-LlmVerdict existence, plan doc amended) -> 553f1f0 ConvertTo-LlmVerdict layered parser + Get-LlmReviewVerdict (mock routes RAW through the REAL parser so layers 1/4 are test-covered; 6 pre-flight parser checks cover layers 2/3) + merge matrix (fixture 24/25) -> e7e962d Hook.ps1 Step 8b + dot-source + conditional hard cap (timeout_ms+2000 when enabled) + Logger -LlmLog (JSONL llm object + text line) — fixture 25/25, regression 962/962 + sandbox 938/938 + codex 17/17 -> 5be292e root config.json block enabled:false + Sync-Fixtures, no-op proof 962/962 again. POC synced in C:\git\cc\deepseek-tester\api-gateway-caller.ps1 (system-prompt rules + layered parser true/false/unusable; NOT a git repo, file saved uncommitted). Fixture log pollution avoided: fullpipe child logs to c:\temp\pretoolhook-llm-review-testlogs\. USER GUIDANCE (2026-08-01): (1) LLM tests stay in the SEPARATE llm-review fixture (fast, zero quota — mock injects verdicts; never fold into the ~1000-case suites); (2) phase-I live testing: temporarily set global_modifying_strictness=strict so the loose gated tier asks locally and local/LLM agree (documented in fixture README + guide §10.5); (3) PHASE II pending: teach the LLM layer the strictness_gated semantics (spec §12; memory llm-second-opinion-phase2).

- LLM-CALL HTTP TEST SUITE (2026-08-01, user gap-callout: "where is the test case for the code calling LLM?"): new test/config/llm-review/http/ — Mock-LlmServer.ps1 (runspace-hosted HttpListener on 127.0.0.1; records every request: method/path/headers/body; programmable status/body/delay) + Run-LlmCallTests.ps1 (attribute-driven runner; REMOVES PRETOOLHOOK_LLMREVIEW_MOCK at startup so the real Invoke-RestMethod path is always exercised; check-token vocabulary for request assertions) + test-llm-call-core.xml (5: happy modifying/read-only, garbage->unusable, HTTP500->down, and Core-FullPipe-HookCallsLlm — spawns the REAL Hook.ps1 against the mock server proving the hook emits the LLM call) + test-llm-call-matrix.xml (20: request shape method/path/ct/model/sys+user roles/command_block tags/cmd text/temp/max_tokens/seed/auth present+absent/8000-char truncation; response variants upper/spaced/JSON/ramble-recovered; dead-port + client-timeout -> down). 25/25 GREEN first run; teeth verified (leaked mock env -> runner disarms and stays green; manual probe: mock env -> 0 server hits, without -> 1). Zero quota — localhost only. NOTE: user's own sandbox file test/config/llm-review/test-cases.1.xml (the 2 merge cases they asked about) + runner pointed at it — their experiment, left untracked/unchanged; canonical test-cases.xml re-verified 25/25 via temp runner copy. Run-AllTests 962/962 after all additions.

- LLM-LIVE E2E SUITE (2026-08-01, user request: "we already have live LLM url, why not a small test case to call that"): new test/config/llm-review/http/Run-LlmLiveTests.ps1 + test-llm-live.xml — 5 opt-in cases against the REAL gateway (glm-5.2 @ 127.0.0.1:3030): preflight GET /v1/models (down => loud exit 1, no quota), 4 direct verdict assertions (local/remote x read-only/modifying; commands only classified, never executed; deliberate few-shot overlap for stability) + 1 fullpipe Get-Date through the real Hook.ps1 => allow. Retry once ONLY on down/unusable (transient); wrong verdicts never retried (model-quality signal). Every verdict printed LIVE with latency/raw. Cost ~1.5k tokens/run. FIRST LIVE RUN: 5/5 GREEN, all 4 direct answers clean bare tokens (no rambling — the new prompt held; latencies 5-12s), fullpipe allow+exit0. Offline suites re-verified before/after: parent 25/25 (canonical), http mock 25/25, Run-AllTests 962/962. Fixture README + project README gained the run instructions.

## Locked decisions (L1-L5)
- L1 name: strictness_gated. L2 scope: third section + per-domain strictness. L3 guard: global strict/loose forces all domains; global normal defers to per-domain. L4 reach: strictness_gated + AWS flag-strip + parameter_commands use effective strictness; path policy stays global. L5 testing: separate test config fixtures via new -ConfigPath param; live config.json untouched by feature tests.

## Design summary
- Get-EffectiveStrictness(Config, Domain): global!=normal → global; else domain's own modifying_strictness; else normal.
- New match step 1a.5 (between read_only and modifying): strictness_gated → ask iff effective==strict, else allow.
- AWS flag-strip (Resolver:180) + param_commands unrecognized (Resolver:890) use effective strictness; path policy unchanged.
- Move list (read_only-with-risk → strictness_gated): Git×10 (pull, switch, init, clone, tag -d, add, worktree add, commit, rev-parse, stash) + Linux printf (cross-domain isolation demo). No per-domain strictness shipped by default → normal behavior byte-identical.
- Fixtures: test/config/config.git-strict.json (Git=strict) + config.strict.json. New suites: test-cases.strictness-gated.{normal,strict,git-strict}.xml.

## Current Step
llm_second_opinion IS LIVE IN THE ROOT CONFIG (2026-08-01, user request "make it be able to use the LLM"): root config.json now has llm_second_opinion.enabled=true, level=complex_remote, timeout_ms=30000 (12s was under glm-5.2's observed 5-12s+ latencies), model=deepseek-v4-flash (glm-5.2's upstream started returning 502 Bad Gateway mid-session - the gateway itself is fine; swap back whenever). global_modifying_strictness=strict per the user's phase-I testing rule (gated tier asks locally so local/LLM mostly agree; revert to normal when phase I ends). Production probe verified end-to-end: `aws s3 ls && kubectl get pods` through the real Hook.ps1 (root config, no overrides) -> allow + exit 0 + log record effect=agree (deepseek-v4-flash, 1489ms); the two earlier glm-5.2 probes correctly produced *** LLM-DOWN *** forced-asks (fail-closed working as designed; 502 surfaced in ~2s, not a timeout). SYNC GUARD ADDED: Sync-Fixtures.ps1 now REFUSES to run while root has llm enabled=true (test copy must never make real LLM calls) or global strictness != normal (962-suite baselines assume normal root) - verified refusing. NOTE: the test/config/live copies therefore currently DIVERGE from root (enabled:false, normal) - deliberate; re-sync when the live config returns to the normal/disabled state. All suites re-verified green before the root edits: fixture 25/25, http 25/25, live E2E 5/5, Run-AllTests 962/962, sandbox 938/938.

## Next Steps
- Manual smoke test (user-run): enable llm_second_opinion + strict mode, exercise an in-scope command in VS Code Copilot, check the records.jsonl llm object; then review disagreement stats over some days of use.
- Phase II discussion (user-flagged): make the LLM layer observe the strictness_gated tier so the strict-mode workaround is unnecessary (spec §12; memory llm-second-opinion-phase2).
- Production model choice (glm-5.2 is the dev stand-in; the prompt is model-agnostic).
- (optional) Sync sandbox test-strictness-gate/config.normal.json from root config.json if you want the sandbox to match the live all-gated state (it currently diverges: lacks the user's Export-File/Set-Location/etc. edits; re-evaluate sandbox case 139 + any siblings after syncing).
- (optional, separate change) Improve Get-CommandDomain so `& <windowspath>` / bare `<drive>:\path\.ps1` invocations detect as powershell instead of falling to the linux default (currently harmless for trusted_programs — the check is domain-agnostic and arg-suffixes re-detect — but cosmetically wrong; re-routes commands so needs its own regression pass).
- Merge strictness-gated → ClaudeCode-v1 ONLY with explicit user approval (branch now contains the major all-gated live-behavior change + this fix).
- Optional follow-ups (non-blocking): config.aws-strict.json fixture for AWS-reach differentiating test; make Evaluate-ParameterRules -Domain mandatory; apply the audit fixes (H1-H6, M1-M4 from docs/finding_1.md) if the user approves them.
- Regenerate test/config/live fixtures from config.json whenever it changes.

## Blockers / Notes
- None — Run-AllTests 921/921 green (KnownFails all 0); sandbox 897/897; codex unaffected (17/17 last run); grep comfyui = 0. User scratch files left untracked by design: config - Copy*.json, config.json.bak, debug/*.ps1.
