## Goal
Maintenance mode: the PreToolUse safety hook (local classifier + llm_second_opinion cross-check) is feature-complete. Current work = fixing production mis-classifications and extending config coverage for new tools.

## Current Step
(none — R1/R2/R3 regex-allowlists-and-editable-delete design fully implemented; full regression 1357/1357 green. Per user: NO merge into master yet — feature_fix soaks for days of regular testing before merging.)

## R1/R2/R3: regex allowlists + editable-path deletion (2026-09-18, DONE, all suites green 1357/1357)
- Design: docs/superpowers/specs/2026-09-18-regex-allowlists-and-editable-delete-design.md. Three requirements, strict red/green TDD (4 red/green phases A–D).
- **R1 — `trusted_programs_regex`**: new config key; regex entries matched AFTER literals miss, against the normalized program token (lowercased, `/`→`\`), unanchored `-match`, case-insensitive. One entry `"src\\\\.*\\.ps1$"` now covers all `src\*.ps1` scripts including future agent-invented names. ConfigLoader compiles to `_compiled.trustedProgramRegexes`; invalid regex throws at load (fail-fast); absent key = byte-identical behavior.
- **R2 — editable-path deletion**: new Resolver step 0g-delete (before tier lookup): when the first token is a deleter verb (remove-item/rm/del/ri/erase/rd/rmdir/unlink/clear-content), extract path targets via the new quote-aware `Split-GuardTokens` tokenizer and apply a 4-row decision ladder: (1) any target matches `system_paths` → ask "delete of system path" (system paths can NEVER be deleted, even under editable_paths — trumps the LLM); (2) variable/`%`/canonical-null/drive-root/UNC-root targets → ask; (3) ALL targets temp-form OR Test-EditableOrCwd → allow tier `editable_delete`; (4) else → ask "delete of non-editable path". No extracted target → fall through to existing tiers. Remote wrappers (ssh/docker exec/kubectl exec/Invoke-Command -ComputerName) force ask via `-IsRemote`.
- **R3 — `dotnet_static_method_allowlist_regex`**: new config key; regex entries tried AFTER the exact set misses, against the written key AND reflected full-name key, unanchored `-match`, case-insensitive. One pattern covers all 7 former `regex::*` statics.
- **LLM integration (R2)**: (a) per-segment tier stamp — a redirect segment resolving read_only with an editable/temp target is upgraded to `editable_path` so the LLM merge can suppress it; (b) attributed merge — `editable_path`/`editable_delete` sub-commands are suppressible like trusted_program, BUT only when `Test-CommandTargetsSystemPaths` (new Parser helper: tokenizes the command text and checks every path-like token against `_systemPathRegex`) returns false — the INV-3 belt-and-braces guard so a system-path target can never be LLM-suppressed; (c) §4.6 skip — when EVERY in-scope sub-command is trusted_program/editable_path/editable_delete, the LLM call is skipped entirely (verdict `not_called`, effect `llm-skipped-trusted-editable`).
- **Live config collapse** (behavior-preserving, verified by smoke test before re-sync): removed 4 `src\*.ps1` literals from trusted_programs (covered by the new regex); removed 7 `regex::*` entries from dotnet_static_method_allowlist (covered by the new static regex). Re-synced test/config/live via Sync-Fixtures.ps1.
- **Test-suite changes**: one live case retargeted — `& src\other.ps1 ...` was a "not in trusted_programs → ask" baseline, but R1's regex now legitimately trusts it; moved the baseline to `c:\temp\other.ps1` (outside src\) and ADDED a new allow case pinning the R1 regex coverage.
- **New suites** (registered in Run-AllTests.ps1): trusted-programs-regex (3), safe-expr-regex (3), editable-delete (16 XML cases via wrapper), editable-delete.llm (34: 6-case LLM sub-suite + existing llm-review runner). Fixtures under test/config/{trusted-programs-regex,safe-expr-regex,editable-delete}/ with their own config.json per CLAUDE.md.
- **Docs**: docs/tier-labels.md (new editable_delete/editable_path sections; LLM merge table rewritten incl. INV-3 guard + §4.6 skip); docs/config-json-guide.md (§2 write+deletion path policy ladder, §3 static regex semantics, new §5b trusted_programs_regex).
- Full pass: 1357/1357 (was 1315/1315 before suite registration; +41 = 40 new cases + 1 retarget split into 2).

## Compound `cd ...; pwsh -File <trusted>` lost trusted_program tier → LLM veto (2026-09-17, DONE, all suites green 1315/1315, +4 new)
- User report: production log 2026-09-17 23:31 — `cd c:\git\cc\pretoolhook; pwsh -NoProfile -File src/Run-AllTests.ps1` forced ask. Local decision was allow with tiers [1]=read_only [2]=read_only, but the LLM flagged [2] as modifying and vetoed it. User expected [2] to be tier=trusted_program (src/Run-AllTests.ps1 IS in trusted_programs), which Option B would suppress → final allow. "I thought we fixed a similar issue not long ago" — that was Option A (pwsh -File collapse), but it only lives in the AST walker, gated on domain==powershell.
- Root cause (verified by probe): the COMPOUND form's domain is LINUX (the leading `cd` decides; no Verb-Noun/PS marker), so the classifier takes the REGEX path, not the AST path. On that path, STEP 4c called Find-NestedCommands on the FULL command string, but every wrapper pattern inside it is start-anchored (^pwsh..., ^ssh..., ...). A string starting with `cd` matches none of them → no -File unwrapping → the whole `pwsh -NoProfile -File src/Run-AllTests.ps1` segment fell to the generic `pwsh` read_only pattern. Two consequences: (a) cosmetic — the script path never got tier=trusted_program, so Option B could not suppress the LLM flag; (b) SAFETY HOLE — `cd ...; pwsh -NoProfile -File C:\temp\untrusted.ps1` was ALLOWED (generic pwsh pattern) instead of asked. The standalone form worked because its domain is powershell → AST path → Option A collapse fires.
- Fix (src/Classifier.ps1 STEP 4c): call Find-NestedCommands per sub-command SEGMENT (from $subCommands) instead of on the full string. For single-segment commands this is identical to the old full-string call; for compound commands the anchored patterns now match the wrapper segment itself. Each nested entry's ParentCommand is the SEGMENT text, so the existing parentTexts filtering in the combination step suppresses exactly that segment (not the whole compound). $ParentDomain was declared but never read inside Find-NestedCommands — safe to pass per-segment domain.
- TestRunner.ps1: optional per-case `subresult-tier` XML attribute (asserts some SubResult carries that Tier; absent on all pre-existing cases => decision-only behavior unchanged). Needed because the trusted compound case produces Decision=allow + Reason="read-only" BOTH broken and fixed — only the tier differs.
- Tests: 3 new PSWrapper-Compound cases in test/config/live/test-cases.xml (cd; pwsh -File trusted → allow + subresult-tier=trusted_program [the incident]; cd; pwsh -File untrusted → ask [safety hole]; cd; powershell -Command "Remove-Item ..." → ask [modifying inner still caught]). 1 new P2-TrustedProgramCompoundSuppressed in test/config/llm-review/test-cases.p2.small.xml (compound ls; pwsh -File trusted; curl — LLM flags the script at idx 3, suppressed by Option B policy → allow; uses ls as first segment because the fixture's minimal config recognizes ls but not cd). RED confirmed: 3 live + 1 llm-review failing pre-fix; GREEN post-fix.
- Full pass: 1315/1315 (all suites, zero regressions — baseline was 1311/1311 at 1c8ca67; +4 = new cases).

## MiobuildHeaders "unknown command": Layer 1 allowlist + Layer 2 atomic unknown (2026-09-17, DONE, all suites green 1272/1272, +5 new)
- User report: production log 2026-09-17 11:01:47 — a purely in-memory Basic-auth header assignment (`$global:MiobuildHeaders = @{ Authorization = 'Basic ' + [Convert]::ToBase64String([Text.Encoding]::ASCII.GetBytes("$($global:MiobuildCredential.UserName):$($global:MiobuildCredential.GetNetworkCredential().Password)")) }`) forced ask (tiers [1]=unclassified [2]=unclassified, reason showed TWO bogus fragments). LLM said read-only but "LLM never downgrades" kept the local ask.
- Root cause (verified by probe, NOT the handoff doc's claim): `GetNetworkCredential()` is an instance method absent from the name-only `dotnet_method_allowlist` => Test-SafeAst fails => safe-expressions path empties AND the AST arbiter cannot certify. The regex Split-Commands fallback then misreads the `:` inside `$($user:$pwd)` interpolation and manufactures 2 phantom fragments, each tier unclassified => ask.
- CORRECTION to handoff doc: the incident command's domain is LINUX, not powershell (assignment-strip leaves `@{ ... }`, no Verb-Noun/PS marker). Consequences: (a) Layer 1 works via the DOMAIN-AGNOSTIC arbiter in STEP 4f, not the powershell-gated safe-expressions path; (b) the approved Layer 2 gate (`domain==powershell`) would have been a NO-OP for all incident cases — user approved Option 1: drop the domain gate.
- Layer 1 (config-only): added `"GetNetworkCredential"` to `safe_expressions.dotnet_method_allowlist` in config.json, config.local.json (live), test/config/live/config.json. Pure read accessor on PSCredential (materializes NetworkCredential from in-memory SecureString; same rationale as existing GetBytes). MiobuildHeaders now => allow "read-only (PowerShell AST arbitration)".
- Layer 2 (src/Parser.ps1 + src/Classifier.ps1): new `Test-PowerShellParses` helper (ParseInput, $errors.Count -eq 0; distinguishes parse-failure from genuinely-zero-cmdlets — both previously returned @() indistinguishably). New combination branch in Invoke-Classify STEP 4b-3: when Get-PowerShellCommands(cmd)==0 AND safeExpressions==0 AND Test-PowerShellParses(cmd) => classify ONE atomic unknown (ask, tier unclassified, reason "unsafe PowerShell expression (fail-closed): <80-char stmt>") instead of regex-splitting. Nested/subshell commands still unioned. Parse failure (bash for-loop) keeps the legacy regex path.
- CRITICAL BUG caught by full regression: first version gated on `$astCommands.Count -eq 0`, but $astCommands is only populated for powershell-domain inputs — so it was @() for ALL non-powershell commands, and since most shell text parses as valid PS syntax, the branch fired for git/curl/docker/while-loops => 17 regressions (5 in test-cases.xml + 8 llm-review.small + 3 phase-I + 1 check-blindspot). Fix: call Get-PowerShellCommands DIRECTLY in the condition (true cmdlet count for any domain; real commands have >=1 CommandAst and are excluded).
- Reason-text refinement: unlisted `[Type]::Method(...)` keeps the specific "static method not on allowlist: ..." reason (unanchored match, guarded by _dotnetStaticMethodAllowlist so an ALLOWLISTED static in a partially-unsafe expression gets the generic wording instead — decision stays ask either way). Tier reuses `unclassified` (no new tier => check_blindspot + LLM merge unchanged).
- Tier upgrade after arbitration (user request 2026-09-17 evening): production log showed MiobuildHeaders allowed but SubResults still displayed tiers [1]=unclassified [2]=unclassified — cosmetic noise, since the arbiter had just proved every statement safe. Root cause: the G5 stamp-back only fills EMPTY tiers from the arbiter TierMap, and pure expressions (atomic entry + subshell fragment) have no CommandAst => no TierMap entry => their pre-arbitration `unclassified` tier survived. Fix (Classifier.ps1 STEP 4f, after stamp-back): when Conclusive=true, upgrade any SubResult tier `unclassified` -> `read_only` (never touches strictness_gated/read_only/safe_expr/redirection). Safe: runs only on the allow path, so no decision/LLM-veto/check_blindspot behavior can change. Verified by probe (tiers now read_only) + full regression 1272/1272.
- TestRunner.ps1: optional per-case `reason-contains` XML attribute (asserts reason text when present; absent on all pre-existing cases => decision-only behavior unchanged).
- Tests: new PSAtomic-Unknown category-group in test/config/live/test-cases.xml (5 cases): MiobuildHeaders exact repro (expect allow, Layer 1), $proc.Kill() + [Console]::Title = 'x' (reason-contains "unsafe PowerShell expression"), $x = [System.IO.File]::Delete(...) (reason-contains "static method not on allowlist"), bash for-loop guard (parse error => legacy path). RED confirmed 4/5 failing pre-fix; GREEN 5/5 post-fix.
- Full pass: 1272/1272 (all 16 suites, zero regressions — baseline was 1267/1267 at a1bd735; +5 = new cases). Verified clean-HEAD baseline via git stash before/after comparison.
- Merge into master (2026-09-17): merged feature_fix into master (which had PR #11 parameter_commands framework, 1306/1306). Only PROGRESS.md conflicted; all code/config auto-merged. Live config.local.json switched to new format (from config.local.git-rework.json) + GetNetworkCredential added.

## Git global-flag misclassification → subcommand framework (2026-09-16, DONE, all suites green 1305/1305)
- User report: production log 2026-09-16 12:57 — `git --no-pager log -1 --format=%B` forced ask (tier unregistered). Git tier patterns are start-anchored; global flags between `git` and the subcommand break them. Step 0d stripped only -C/-c/--git-dir/--work-tree/--namespace/--exec-path, not --no-pager.
- Design (spec: docs/Updates/Update-2026-09-16-parameter-commands-subcommand-framework.md, SPEC v3 FINAL): extend parameter_commands with subcommand rules (phrase = prefix of positional sequence), compound rules (subcommand + flag), global_value_flags (dash forms; walker consumes value token unconditionally), strictness_gated rule decision (entry-level modifying_strictness, global forces), no-positional fallback = truly-bare→allow(usage) / flags-without-subcommand→ask(unregistered, fail-closed) [post-review correction], unknown positional → ask tier unregistered (reason text unchanged for check_blindspot compat).
- New LLM switch llm_second_opinion.strict_gate_override_llm (default false): true = gated sub-commands unconditionally suppress LLM flags (trusted_program-style); false = today's path-guard reconciliation.
- Git migrates to Linux.parameter_commands.git; Parser.ps1 ^git\b routing line removed (git falls through to linux domain). 1:1 rule mapping of all current tier entries + ONE new GATED flag rule (--output, long form only due to -o/-O lowercase collision); git fetch stays read-only; bare git → allow.
- /flag (DOS) support DROPPED from the framework per user decision (leading-path ambiguity on Linux tools); pre-existing DOS flag-rule branch untouched.

### Code changes (green phase)
- src/Parser.ps1: removed `@{ Pattern = '^git\b'; Domain = 'git' }` from $script:KnownBinaryPrefixes (comment left explaining why).
- src/Resolver.ps1: (a) Get-EffectiveStrictness gained optional -Entry param (global forces → entry modifying_strictness → domain → normal); existing callers unaffected. (b) Step 0d git global-flag stripping block REMOVED (superseded by global_value_flags walker). (c) line-801 unregistered-fallback domain list trimmed 'git' → @('docker','kubernetes','terraform'). (d) Get-ShellParameterMap: opt-in subcommand/positionals capture (only for entries with a subcommand rule) — separate walk before the unchanged flag-map loop; dash-tokens skipped, global_value_flags consume next token unconditionally, first non-dash = subcommand, all positionals → reserved '_positionals'. (e) New Test-RuleMatch helper (subcommand prefix AND/OR param condition). (f) Evaluate-ParameterRules rewritten: eval order modifying → strictness_gated → read-only → fallback; gated uses entry-aware effective strictness; no-match for subcommand entries = bare→allow(usage) / positional→ask(unregistered); flag-only entries keep legacy unrecognized-value + default path.
- src/ConfigLoader.ps1: parameter_commands validation extended (rule forms A/B/C; decision vocabulary += strictness_gated; entry modifying_strictness ∈ strict/normal/loose; global_value_flags = dash-tokens). Parse llm_second_opinion.strict_gate_override_llm (bool, default false) → _compiled.llmSecondOpinion.StrictGateOverrideLlm.
- src/LlmReview.ps1: attributed merge loop — for a flagged strictness_gated sub-command, StrictGateOverrideLlm=true ⇒ unconditional suppress (trusted_program-style, path guard bypassed); false/absent ⇒ today's Test-GatedInvocationSafe reconciliation. trusted_program + index-0 unchanged.
- test/config/llm-review/Run-Tests.ps1: per-case strict_gate_override="true|false" attribute → sets $llmCfg.StrictGateOverrideLlm (Add-Member -Force, mirrors AttributedVerdicts).

### Config migration (7 files)
Migrated git tier lists → Linux.parameter_commands.git and removed the now-dead top-level Git domain: config.json (root), test/config/live/config.json + config.strict.json (regenerated via Sync-Fixtures.ps1 from root), test/config/llm-review/config.json, test/config/test-strictness-gate/normal/config.normal.json, test/config/test-strictness-gate/strict/config.strict.json, config.local.json. NOT touched: config - Copy.json / config.local - Copy.json (user backups), llm-review config.badlevel/badtype.json (negative fixtures).

### TDD
- RED (pre-fix): git-param normal 6 fails (original failure, bare git, --version, case-insensitive LOG, logg→allow, ssh-wrapped); git-param strict 2 fails (--no-pager log, diff --output→allow); llm-review SwitchTrueSuppresses fail (DefaultDenies guard passed as designed).
- GREEN: git-param normal 26/26, strict 10/10, llm-review small 45/45.
- Regression: full Run-AllTests.ps1 = 1305/1305.
- Post-review correction (user challenge): `git --online -3` is NOT read-only — it's an invalid command (--online is a typo of --oneline, no subcommand). The first GREEN pass wrongly allowed any no-positional invocation as "usage" and I had flipped the two pre-existing flag-only cases (git -C C:\repo --online -3, git --online -3) to allow. Fixed: Get-ShellParameterMap now tracks _hadFlags; the step-4 fallback distinguishes truly-bare `git` (no flags → allow, usage) from flags-without-subcommand (→ ask, tier unregistered, fail-closed). Both cases reverted to their original expected="ask". Added a dedicated git-param case (`git --online -3` → ask) to pin the new behavior. Re-ran full regression: 1306/1306.

### Roadmap
Phase 1 (git pilot) DONE. Phase 2 = terraform → Linux.parameter_commands.terraform (+ remove ^terraform\b routing, drop Terraform section). Phase 3 = docker/k8s/npm/gh/etc. Phase 4 = rename Linux domain to a neutral name (e.g. ExternalTools).

## Type-cast assignment `[type]$var = ...` mis-routed to linux (2026-09-16, DONE, all suites green 1267/1267, +6 new)
- User report: production log 2026-09-16 — a purely read-only Jenkins config fetch (`[xml]$ChildConfig = Invoke-RestMethod -Method Get ... ; [pscustomobject]@{...} | ConvertTo-Json`) forced ask. LLM said read-only but "LLM never downgrades" kept the local ask. Local tiers: [1]=unclassified [2]=unclassified [3]=read_only.
- Root cause: `Get-CommandDomain` Step 0 strips `$var = ` prefixes, but for a TYPE-CAST assignment it left the `[xml]` cast glued to the cmdlet (`[xml]Invoke-RestMethod ...`), so the start-anchored Verb-Noun match failed and no other PowerShell marker fired => whole-command domain fell back to LINUX. With domain=linux the AST walker never runs (Classifier.ps1:558 gate); each raw segment is classified against Linux patterns => the type-cast statement and the [pscustomobject] data expression both become "unknown command" (unclassified) => ask. The AST walker itself handles the command perfectly when fed directly (extracts Invoke-RestMethod, skips the data expression) — only the domain gate was wrong.
- Fix (src/Parser.ps1, Get-CommandDomain Step 0): extended the assignment-strip semantic to also strip a leading `[type]$var = ` prefix via anchored regex `^\[[\w.]+\]\s*\$[\w:]+\s*=\s*` (checked before the plain `$var =` strip). Same meaning as the existing strip: the LHS is not a command, the RHS decides the domain (`[int]$x = git status` routes to git, like `$x = git status`). Anchored at ^ and requires `$var =` right after `]`, so bash `[token]` (test builtin / [[ ... ]]) can never match; generic casts ([List[string]]$x) don't match and stay fail-closed.
- Tests: new suite test/config/live/test-cases.typecast-domain.xml (6 cases, wired into Run-AllTests.ps1 $suiteConfig with -ConfigPath test\config\live\config.json): exact log repro (short URL https://server/config.xml), simpler single-statement form, modifying-RHS guard ([xml]$x = Remove-Item => ask), static-member guard ([Console]::Title = 'x' => ask, unaffected), plain-assignment regression pin ($x = git status => allow), generic-cast fail-closed guard. RED confirmed 2/6 failing pre-fix; GREEN 6/6 post-fix.
- Merge (same day): user reviewed both data-expression suites and merged them into test/config/live/test-cases-regression.xml (11 cases: PSCustomObject-ScriptBlock group + TypeCast-Domain group); the two old files (test-cases.pscustomobject-scriptblock.xml, test-cases.typecast-domain.xml) were deleted and Run-AllTests.ps1 $suiteConfig now lists test-cases-regression.xml. Full run still 1267/1267.
- Full pass: 1267/1267 (all 16 suites, zero regressions — main test-cases.xml 781 incl. AST-Arbiter-Guard).

## Option B: `trusted_program` gets LLM-veto suppression power (2026-09-16, DONE, all suites green 1261/1261, +3 new)
- User request: "give trusted_program suppression power in the merge, mirroring strictness_gated. I already give the trusted_program; these should override the remote LLM."
- Root cause (pre-fix): in the attributed LLM merge (LlmReview.ps1 ~line 826), ONLY `strictness_gated` sub-commands could suppress an LLM flag (via Test-GatedInvocationSafe). A `trusted_program` sub-command the LLM called "modifying" was ALWAYS vetoed => forced ask. So a command mixing a whitelisted script with another sub-command (>= min 2) still hit the ~11s LLM round-trip and a false veto on the trusted part.
- Fix (src/LlmReview.ps1, attributed merge loop): a `trusted_program` sub-command is now UNCONDITIONALLY suppressible (checked before the gated branch). Rationale: the user explicitly whitelisted it in trusted_programs AND the local engine already validated it via Test-StatementContainsModifying (no embedded modifying command) before stamping the tier — that explicit trust overrides a remote LLM veto. No path guard is applied to trusted (the whitelist IS the safety decision; gating it would contradict it). `strictness_gated` still goes through Test-GatedInvocationSafe as before; index 0 and all other tiers (read_only, unclassified, ...) still veto.
- Logger (src/Logger.ps1): reconciliation labels were hardcoded to `(strictness_gated)`. Added Get-SuppressedTierLabel (derives the label from $LlmLog.tiers per suppressed index) so a trusted suppression reads `all suppressed (trusted_program policy)` / `suppressed=[N](trusted_program)`. Backward-compatible: all-gated still renders `strictness_gated` (existing fullpipe log assertions in p2.large.xml unaffected).
- Tests (test/config/llm-review/test-cases.p2.small.xml, 3 new): P2-TrustedProgramSuppressed (trusted sub flagged idx:1 => suppressed => allow; RED pre-fix), P2-TrustedProgramReadOnlyStillVetoes (read_only sub flagged idx:2 => veto => ask; guard, passes pre+post), P2-TrustedProgramFullPipeSuppressed (fullpipe end-to-end + pins the `all suppressed (trusted_program policy)` log label). Uses the fixture's trusted_programs entry src\Run-AllTests.ps1 (added in Option A). RED confirmed: 2 failing pre-fix; GREEN 43/43 post-fix.
- Full pass: 1261/1261 (all 15 suites, zero regressions — phase-I 46 + check-blindspot 35 confirm existing suppression behavior intact).

## Option A: collapse `pwsh -NoProfile -File <script>` into ONE sub-command (2026-09-16, DONE, all suites green 1258/1258, +2 new)
- User report: a standalone trusted-script run `pwsh -NoProfile -File "…\src\Run-AllTests.ps1"` triggered an ~11s LLM round-trip and a false veto (forced ask). The command is purely local + trusted; it should just allow.
- Root cause: the AST walker (`Get-AstCommands`) double-counted the wrapper. For `pwsh -File <path>` it added BOTH the extracted script path (sub-command 1, tier=trusted_program) AND the outer `pwsh -NoProfile -File …` text (sub-command 2, tier=read_only via the `pwsh *` pattern). 2 ≥ complex_min_subcommands(2) => in LLM scope. The LLM generically answered "running a script is modifying" for both; only strictness_gated can suppress an LLM flag, so both were vetoed (log: flagged=[1,2] veto=[1,2]) => ask + latency. Note the regex fallback path already drops the wrapper (it filters sub-commands whose text equals a nested command's ParentCommand) — the AST path did not.
- Fix (src/Parser.ps1): (1) `Get-AstWrapperInnerCommands` -File branch now tags its result `IsTerminal=$true` (the script path IS the command; powershell.exe -File consumes everything after it as $args). (2) `Get-AstCommands` tracks `$suppressOuter`; when a wrapper yields a terminal extraction it SKIPS the "add the outer command itself" AddResult. Net: standalone trusted-script run = 1 sub-command => below min=2 => out of LLM scope => local trusted_program allow stands, no prompt. Surgical: only the -File terminal form is affected; -Command/-ScriptBlock/bash -c/etc. keep their outer entry (IsTerminal absent => $false).
- Security preserved: an UNTRUSTED `pwsh -File <path>` still fails closed to ask (the bare path is unclassified/unknown) — matches the 2026-08-07 -File security fix. Dropping the INNER instead of the outer would have re-opened that hole, so we suppress the outer and keep the path (which carries the trusted_programs gate).
- Tests (test/config/llm-review): added `trusted_programs: ["src\\Run-AllTests.ps1"]` to config.json fixture; 2 new cases in test-cases.p2.small.xml — P2-PwshFileTrustedSingleSub (expected allow, in-scope=false, verdict=not_called, effect=none) and P2-PwshFileUntrustedGuard (expected ask, fail-closed). RED confirmed: trusted case failed pre-fix (got ask via LLM-VETO on sub-command 1); GREEN 40/40 post-fix.
- Full pass: 1258/1258 (all 15 suites, zero regressions — main test-cases.xml 781 pins -Command/-ScriptBlock wrapper behavior).

## rg (ripgrep) read-only: wrong-mechanism config entry fixed (2026-09-15, DONE, live config verified)
- User report: production log 2026-09-15 16:46:52 — `rg "stage\('|^###[0-9]+\." "c:\temp\file.txt" "c:\temp\file2.md"` forced ask (tier=unclassified, "unknown command"). LLM said read-only but check_blindspot kept the local ask.
- Clarified for user: the log's "Pipeline: rg … -> ^###[0-9]" text is a COSMETIC display fallback in Classifier.ps1 (~line 728) that naively splits the raw string on `|` for human-readable annotation only — it does NOT affect the decision. Split-Commands (quote-aware) correctly yields 1 segment, IsPipeline=False. Verified by feeding the exact command through Get-CommandDomain/Split-Commands/Resolve-Command (no rg executed).
- User's first attempt was WRONG and BROKE THE HOOK: added `parameter_commands.rg = { aliases:["rg.exe"], default:"read-only" }` to config.local.json. ConfigLoader.ps1:484 validates that every parameter_commands entry has a NON-EMPTY rules array and THROWS on load; Hook.ps1 exits 2 on any config error => EVERY command hard-blocks (not just rg). parameter_commands is the per-parameter mechanism (curl/python) and cannot express "always read-only".
- Fix (config.local.json, Linux domain): removed the broken parameter_commands.rg entry; added a read_only pattern `{ "name": "rg", "patterns": ["rg *","rg","rg.exe *","rg.exe"], "description": "ripgrep search (read-only)" }` next to grep.
- Verified: config.local.json loads+validates clean; exact production command + `rg pattern file` + `rg --version` + `rg.exe foo bar` all => allow/read_only; grep regression still allow. Temp diag/verify scripts removed.
- Note: this is a live-config-only change (no code, no test-suite impact). rg is read-only in ALL forms so a read_only pattern is the correct mechanism (not parameter_commands).

## LLM verdict parser: stray double-quote in prose -> unusable (2026-09-15, DONE, all suites green 1256/1256, +3 new)
- User report: production log 2026-09-15 14:00:24 — a purely read-only `Select-String ... | Select-Object` command was forced to ask. Local said allow (read_only ×2), LLM answered `{"modifying":[]}` (read-only), but the response parsed as verdict=unusable -> fail-closed ask.
- Root cause: ConvertFrom-BalancedJsonObject (src/LlmReview.ps1) tracks string state using DOUBLE quotes only. The LLM's analysis prose echoed a command containing `'rg "stage'` — a double-quote inside single-quoted text. That stray `"` flipped the scanner into permanent "inside string" mode, so the valid `{"modifying":[]}` glued at the end was treated as string content (depth never incremented) -> 0 candidate objects -> Layer 3.5a chatty-model rescue found nothing -> unusable. Probe confirmed: exact raw => 0 objects/unusable; same raw with the stray `"` removed => 1 object/read-only(recovered).
- Fix (src/LlmReview.ps1, ConvertFrom-BalancedJsonObject): when the quote-aware primary scan finds ZERO objects, run a second quote-blind balanced-brace scan (count {/} depth, ignore quotes) as fallback. Conservative: fires only on zero candidates, never overrides a successful quote-aware extraction. Candidates still pass ConvertFrom-Json + Test-ModifyingArray schema validation in Layer 3.5a, so prose fragments are rejected — the scanner only proposes, the JSON parser disposes. Rescued verdicts stay Recovered=$true.
- Tests: 3 new LlmParser-* cases in test/config/llm-review/Run-Tests.ps1 $parserCases — StrayDqProseGluedRO (read-only rescue), StrayDqProseGluedMod (modifying rescue idx 2), StrayDqNoJson (safety guard: stray `"` but no JSON/token must stay unusable). RED confirmed 2/3 failing pre-fix (the guard already passed); GREEN 38/38 post-fix.
- Full pass: 1256/1256 (all 15 suites, zero regressions).

## [pscustomobject]@{...} in Invoke-Command -ScriptBlock: phantom unclassified fix (2026-09-15, DONE, all suites green 1247/1247, +5 new)
- User report: production log from another machine (2026-09-03) showed a purely read-only command (`Invoke-Command -Session ... -ScriptBlock { [pscustomobject]@{ Test-Path ...; Get-Service ... } }`) decomposed into 8 sub-commands, two of which were `unclassified` (the raw `[pscustomobject]@{...}` text), forcing an unnecessary ask. LLM second-opinion said read-only but "LLM never downgrades" kept the local ask.
- Root cause: double-extraction. The AST walker (`Get-PowerShellCommands`) correctly decomposes the ScriptBlock into its constituent cmdlet invocations (Test-Path, Get-Service) and treats `[pscustomobject]@{...}` as a data expression (hashtable literal), NOT a command. But `Find-NestedCommands` (regex) ALSO matches `Invoke-Command ... -ScriptBlock { ... }` and dumps the entire ScriptBlock content as a raw "command" text, then re-splits it — producing phantom entries that fall through to "unknown command" (tier=unclassified).
- Fix (src/Classifier.ps1, Invoke-Classify combine step): when AST extraction succeeds (`$astCommands.Count -gt 0`), do NOT append `$nestedCommands`. The AST walker already handles all wrapper cases natively (Get-AstWrapperInnerCommands + ScriptBlockAst recursion). `Find-NestedCommands` is a regex fallback for when AST parsing is unavailable; when AST succeeds it only adds phantom entries from data expressions. Fallback paths (AST failed / safe-expressions / no-AST) unchanged.
- Tests: new suite test/config/live/test-cases.pscustomobject-scriptblock.xml (5 cases): exact log repro, simpler form, modifying-inside-hashtable (still asks), nested pwsh -Command inside [pscustomobject], [pscustomobject] + pipeline. RED confirmed 4/5 failing pre-fix; GREEN 5/5 post-fix.
- Full pass: 1247/1247 (all 15 suites, zero regressions).

## (Archived) Earlier history (pre-2026-09-15)
- All entries before 2026-09-15 (July-August: strictness_gated + per-domain strictness, tool-gate v2 / system_paths absolute, local-vs-generic config split, DSH wiring, ask_notification, check_blindspot, LLM second-opinion phases I-III, parser/verb/static-allowlist fixes, security audits) were removed from this file on 2026-09-15 to keep it current. Details live in the user's PROGRESS.md backup and in git history (PROGRESS.md was committed with each change).

## Next Steps
- Verify full regression on the merged master tree (framework 1306 baseline + 5 PSAtomic-Unknown cases); update Run-AllTests baselines if counts shifted.
- Push merged master to origin.
- parameter_commands framework Phase 2 = terraform → Linux.parameter_commands.terraform (+ remove ^terraform\b routing, drop Terraform section). Phase 3 = docker/k8s/npm/gh/etc. Phase 4 = rename Linux domain to a neutral name (e.g. ExternalTools).

## Blockers / Notes
- None. Live hook reads config.local.json via PRETOOLHOOK_CONFIG_PATH; it loads clean and now includes GetNetworkCredential in dotnet_method_allowlist.
