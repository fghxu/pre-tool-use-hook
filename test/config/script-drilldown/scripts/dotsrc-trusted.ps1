# dotsrc-trusted.ps1 - dot-sources the TRUSTED script (9b $null branch -> kept statement,
# Domain forced 'powershell' (F12) -> trusted_program allow). Pins the F12 fix.
. scripts\trusted.ps1
Write-Host ok
